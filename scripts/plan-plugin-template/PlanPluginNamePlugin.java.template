package net.runelite.client.plugins.microbot.__PACKAGE_SUFFIX__;

import com.google.inject.Provides;
import net.runelite.client.config.ConfigManager;
import net.runelite.client.plugins.Plugin;
import net.runelite.client.plugins.PluginDescriptor;
import net.runelite.client.plugins.microbot.agentserver.controlcenter.ControlCenterPlugin;

import javax.inject.Inject;

@PluginDescriptor(
        name = PluginDescriptor.Plan + "__PLUGIN_NAME__",
        description = "Planned __PLUGIN_NAME__ automation",
        tags = {"plan", "microbot", "automation"},
        enabledByDefault = false
)
@ControlCenterPlugin(id = "__CONTROL_CENTER_ID__")
public class Plan__PLUGIN_NAME__Plugin extends Plugin
{
    @Inject
    private Plan__PLUGIN_NAME__Script script;

    @Inject
    private Plan__PLUGIN_NAME__Config config;

    @Provides
    Plan__PLUGIN_NAME__Config provideConfig(ConfigManager configManager)
    {
        return configManager.getConfig(Plan__PLUGIN_NAME__Config.class);
    }

    @Override
    protected void startUp()
    {
        script.run(config);
    }

    @Override
    protected void shutDown()
    {
        script.shutdown();
    }
}
